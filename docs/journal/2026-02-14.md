### 07:53 - Add React ErrorBoundary component with tests and Storybook story [claude-sonnet-4-5-20250929]

**User Prompt:**
Implement the following plan: React Error Boundaries — wrap the app and per-route outlet in ErrorBoundary components so render-time errors show a user-facing fallback instead of a blank screen.

**Issues Encountered:**
- Three pre-existing test failures in `AssessmentGradeStore.test.ts`, `AssessmentStore.test.ts`, and `RecentAssessmentRepository.test.ts` caused by `window.location.hash` being undefined in their test environment; these are unrelated to this change.

**Decisions Made:**
- Used a class component for `ErrorBoundary` because React's error boundary API (`getDerivedStateFromError`, `componentDidCatch`) is only available on class components.
- Applied two boundary levels: one at the top of the app in `main.tsx` (catches everything) and one around `<Outlet />` in `RootLayout.tsx` (keeps the sidebar/header functional when a route throws).
- Used Ant Design `<Result status="error">` for the fallback UI to stay consistent with the existing component library.
- All UI text written in German as required by project conventions.

**Technical Info Consulted:**
- Existing test patterns in `GradeInputComponent.test.tsx`
- Existing Storybook patterns in `GradeInputComponent.stories.tsx`

**Assumptions Made:**
- The `Object.defineProperty` approach for mocking `window.location.reload` is sufficient for the unit test.

**Other Notes:**
- All 4 new `ErrorBoundary` tests pass. TypeScript, Prettier, and ESLint checks pass cleanly.

### 07:13 - Deploy Storybook to GitHub Pages alongside the UI [claude-sonnet-4-5-20250929]

**User Prompt:**
Implement the plan to deploy Storybook to GitHub Pages alongside the UI under the `/storybook/` subpath.

**Issues Encountered:**
- None

**Decisions Made:**
- Added a single `Build Storybook` step in `.github/workflows/ui-deploy-pages.yml` after the main Vite build and before `Setup Pages`
- Output directory set to `dist/storybook` so it lands inside `ui/dist/` which is already the artifact root

**Technical Info Consulted:**
- Existing workflow structure in `.github/workflows/ui-deploy-pages.yml`

**Assumptions Made:**
- Storybook's built output uses relative asset references, so no base-path configuration is needed when serving from a subdirectory

**Other Notes:**
- After the workflow runs, Storybook will be accessible at `<pages-url>/storybook/index.html`
- The main app remains at `<pages-url>/`

### 07:05 - Fix CI test failures caused by stale Jestor store state across test files [Claude Sonnet 4.5 claude-sonnet-4-5-20250929]

**User Prompt:**
CI test failure: ClassOverview unable to find "Mathe" after rendering

**Issues Encountered:**
- With `isolate: false` in Vitest, Jestor store module-level singletons persist across test files
- The `hasInitialized` guard in `jestor.ts` prevents data-loading stores from triggering `init` (i.e. `loadAll`) after `clearAllRepositoryCaches()` resets the TinyBase/IndexedDB layer
- Tests that run after other tests that used the same store would see stale (empty) entities
- Naive "reset all stores" approach reset `databaseStore` too, wiping out `setDatabaseMode('example')` mid-test and breaking `exampleDatabaseSeed.test.ts`

**Decisions Made:**
- Added a global `jestorStoreResetters` registry in `jestor.ts` (mirroring `repositoryCacheClearers` in `createRepository.ts`)
- Each store with an `init` function registers a `reset()` that restores `baseState` and clears `hasInitialized`
- Configuration stores without `init` (like `databaseStore`) are intentionally excluded from the reset registry
- `clearAllRepositoryCaches()` now calls `resetAllJestorStores()` before clearing TinyBase stores

**Technical Info Consulted:**
- `jestor.ts` `createStore` internals, `hasInitialized` guard, `runInit()`
- `createEntityStore.ts` — entity stores with `autoLoad: true` have an `init` hook
- `databaseStore.ts` — no `init` hook, must not be reset

**Assumptions Made:**
- Stores without an `init` function are configuration stores that should not be reset between tests
- Resetting before `destroyAllTinyBaseStores` is safe because no React components are mounted during `beforeEach`

**Other Notes:**
- None

### 06:50 - Fix CI test failure due to missing git-info.ts in CI [Claude Sonnet 4.5 claude-sonnet-4-5-20250929]

**User Prompt:**
Another test failure: `Failed to resolve import "../../git-info"` in Dashboard.test.tsx

**Issues Encountered:**
- `src/git-info.ts` is in `.gitignore` (auto-generated, never committed)
- `generate-git-info.js` only runs as a `prebuild` hook, not before tests
- Vite's `import-analysis` plugin fails at test compile time even for dynamic imports
- Locally the file exists from a prior build; in CI it is never generated

**Decisions Made:**
- Added a `Generate git info` step to `ui-ci.yml` that runs `node scripts/generate-git-info.js` before `pnpm test:ci`
- This mirrors the `prebuild` hook and makes CI match the local environment

**Technical Info Consulted:**
- `package.json` `prebuild` script
- `.gitignore` confirming `src/git-info.ts` is excluded
- `.github/workflows/ui-ci.yml`

**Assumptions Made:**
- The generate script is safe to run in CI (it only reads git metadata and writes a TS file)

**Other Notes:**
- None

### 06:35 - Fix CI test failures caused by waitFor timeout [Claude Sonnet 4.5 claude-sonnet-4-5-20250929]

**User Prompt:**
There are test failures for test:ci in github CI, but not locally. Before fixing them, figure out to ensure github and local execution is the same.

**Issues Encountered:**
- `AssessmentPage.test.tsx` and `StudentGradesPage.test.tsx` failing in CI but not locally
- Tests were timing out at exactly 1052ms and 1159ms respectively — just over the default `waitFor` timeout of 1000ms
- CI runners have slower CPUs and I/O than local dev machines, causing async TinyBase/IndexedDB operations to take longer

**Decisions Made:**
- Set a global `asyncUtilTimeout: 5000` via `configure()` from `@testing-library/react` in `testSetup.ts`
- This applies consistently to all tests in both environments, making tests resilient to slow CI without changing test logic

**Technical Info Consulted:**
- `@testing-library/react` `configure()` API for `asyncUtilTimeout`
- `vite.config.ts` vitest config: `isolate: false`, `setupFiles: ['./src/testSetup.ts']`
- `testSetup.ts`: global `beforeEach`/`afterEach` hooks for IndexedDB reset and DOM cleanup

**Assumptions Made:**
- 5000ms is a sufficient upper bound for async repository operations even on slow CI runners
- The root cause is purely timing (CI slowness), not a logic bug in the stores or repositories

**Other Notes:**
- The evidence: test durations exceeded the 1000ms default by only 52ms and 159ms, confirming a marginal timing issue rather than a functional defect
