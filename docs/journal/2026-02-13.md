### 07:32 - Fix IndexedDB blocked on Tabula Rasa database deletion

**User Prompt:**
Help me debug the following issue: When deleting the database via tabula rasa, it is blocked on indexeddb. One theory that React calls the resetExampleDatabase function twice simultaneously.

**Issues Encountered:**
- `indexedDB.deleteDatabase()` was firing the `onblocked` event and never completing
- The previous `onblocked` handler silently resolved the promise early, meaning the code continued before the database was actually deleted — causing writes into a stale, still-open connection

**Decisions Made:**
- Added `db.onversionchange = () => db.close()` in `IndexedDBRepository.initDB()`. This is the standard IndexedDB pattern: when a delete (or version upgrade) is requested, all open connections receive a `versionchange` event and should close themselves to unblock the operation.
- Changed the `onblocked` handler in `resetExampleDatabase()` to log a warning and keep the promise pending (instead of resolving early), so the code only continues after `onsuccess` fires — i.e., after the database is truly gone.
- The React double-call theory was investigated and ruled out: `handleExampleReset` is a button click handler (not a `useEffect`), so React StrictMode double-invocation does not apply. The `dbSwitching` guard also prevents double-clicks.

**Technical Info Consulted:**
- MDN IndexedDB: `IDBDatabase.onversionchange` — connections must close when they receive this event to allow pending deletes/upgrades to proceed.

**Assumptions Made:**
- All repositories connect through `IndexedDBRepository.initDB()`, so adding the handler there covers all open connections uniformly.

**Other Notes:**
- All 214 tests pass after the fix.

### 07:43 - Add fullscreen progress overlay during Tabula Rasa and DB switch

**User Prompt:**
Implement a progress overlay so it's clear something is happening when clicking "Tabula Rasa" or toggling the example database.

**Issues Encountered:**
- None

**Decisions Made:**
- Used Ant Design's `Spin` component with the `fullscreen` prop (available in Ant Design 5.x) to show a fullscreen overlay with a German tip message while `dbSwitching` is true
- Added a separate `resetting` state so the overlay only appears during Tabula Rasa, not when simply toggling the example database switch

**Technical Info Consulted:**
- Ant Design `Spin` component documentation (fullscreen prop)

**Assumptions Made:**
- The existing `dbSwitching` state already correctly tracks the duration of database operations

**Other Notes:**
- All 214 tests pass after the change.

---

### 17:30 - Replace IndexedDBRepository with RxDB

**User Prompt:**
Replace the hand-rolled IndexedDBRepository (~470 lines) with an RxDBRepository class that uses the RxDB library over IndexedDB (via Dexie storage adapter), keeping the same Repository<T, TCreate> interface.

**Issues Encountered:**
- RxDB v16 TypeScript types only allow `additionalProperties: false` in JSON schemas, but we need `true` since Zod handles validation — worked around with type assertion `true as unknown as false`
- RxDB's Dexie storage requires all indexed fields to be in the schema's `required` array (DXE1 error)
- Schema mismatch errors (DB6) in tests when the same database/collection name was used with different schemas across test suites — fixed by making all test configs use consistent schemas
- Data leaking between tests because `db.close()` preserves data and Dexie's internal caches survive IDBFactory replacement — fixed by using `db.remove()` (which deletes all data) in `destroyAllRxDatabases()`

**Decisions Made:**
- Created `RxDBRepository.ts` implementing the existing `Repository` interface via RxDB's `createRxDatabase` and `addCollections` APIs
- Module-level `databaseCache` (Map<string, Promise<RxDatabase>>) prevents duplicate database creation across repositories sharing the same database
- RxDB JSON Schema is permissive (`additionalProperties: true`) — we rely on Zod for data validation, not RxDB's schema
- Generic Date serialization: all Date fields are converted to ISO strings on write and back to Date on read (not just createdAt/updatedAt), handling entities like Assessment with a `date` field
- `inTransaction` becomes a no-op shim since RxDB manages transactions internally
- `ensureNotenbankStores` becomes a no-op since RxDB manages collections lazily via `db.addCollections()`
- `clearAllRepositoryCaches()` is now async — it must `await destroyAllRxDatabases()` before clearing repository instance maps
- Created a global `testSetup.ts` (via Vitest setupFiles) that resets IDBFactory and clears all caches before each test
- Kept `IndexedDBRepository.ts` and its tests for reference, just no longer wired up

**Technical Info Consulted:**
- RxDB v16 documentation: `createRxDatabase`, `addCollections`, `RxJsonSchema`, `bulkInsert`, `doc.patch()`, `db.remove()`, `removeRxDatabase`
- RxDB uses Dexie internally for the `getRxStorageDexie()` adapter
- `fake-indexeddb` provides the in-memory IndexedDB implementation for tests

**Assumptions Made:**
- RxDB doesn't enforce uniqueness on compound indexes (the assessmentGrades `['assessmentId', 'studentId']` unique constraint) — application logic already prevents duplicates
- The `onUpgrade` callback in repository configs is unused by RxDB but kept for API compatibility

**Other Notes:**
- All 214 tests pass after the change
- `pnpm run check` (typecheck + prettier + eslint + tests) passes
- `pnpm build` succeeds
- Files changed: created `RxDBRepository.ts`, modified `createRepository.ts`, `notenbankDb.ts`, `exampleDatabaseSeed.ts`, `testSetup.ts`, `vite.config.ts`, `createRepository.test.ts`, and ~20 test files (replaced IDBFactory setup with clearAllRepositoryCaches)

---

### 17:30 - Migrate from RxDB to TinyBase

**User Prompt:**
Replace the rxdb implementation with a tinybase repository implementation

**Issues Encountered:**
- TinyBase's `getRow()` returns an empty object `{}` for non-existent rows (not `undefined` or `null`), which caused `findById` to fail validation when looking up deleted/non-existent entities. Fixed by checking `Object.keys(row).length === 0`.
- TypeScript type mismatches when passing destructured row data to `store.setRow()`. TinyBase expects `Record<string, string | number | boolean>` but we had `Record<string, unknown>`. Fixed with explicit type casts.
- ESLint error for unused destructured variable `_` in update method. Fixed by renaming to `_id` and using `void _id`.

**Decisions Made:**
- Used TinyBase's `store.transaction()` for `createMultiple()` bulk operations to ensure atomicity and minimize listener notifications
- Enabled auto-save on all persisters for automatic persistence without manual intervention
- Kept the same Repository<T, TCreate> interface to ensure all existing entity repositories work without changes
- Maintained multi-database support (primary/example/temporary) via the existing lazy singleton pattern
- Preserved date serialization logic (Date ↔ ISO string) since TinyBase stores JSON

**Technical Info Consulted:**
- TinyBase documentation for `createStore()`, `createIndexedDbPersister()`, and `store.transaction()` APIs
- Verified TinyBase `getRow()` behavior through testing

**Assumptions Made:**
- Bundle size reduction (~88kB) is worth the trade-off of losing built-in indexing
- Current entity counts are small enough that in-memory queries are sufficient
- Compound unique constraints (like AssessmentGrade's [assessmentId, studentId]) will be enforced at application level if needed

**Other Notes:**
- Deleted: RxDBRepository.ts (326 lines), IndexedDBRepository.ts (469 lines), IndexedDBRepository.test.ts (389 lines)
- Created: TinyBaseRepository.ts (~330 lines) with equivalent functionality
- Updated: createRepository.ts, exampleDatabaseSeed.ts, testSetup.ts, notenbankDb.ts, package.json
- All 188 tests pass, bundle size reduced by ~88kB (RxDB ~100kB → TinyBase ~12kB)

---

### 18:00 - Create generic entity store factory

**User Prompt:**
create the generic store factory

**Issues Encountered:**
- Initial test failures due to auto-load behavior: The factory auto-loads entities on store initialization, but tests were expecting empty initial state. Fixed by updating test setup to wait for auto-load completion before asserting on initial state.
- TypeScript errors with implicit 'any' types in test files when resetting store state. Fixed by adding explicit type annotations.
- `Jestor` interface was not exported from `jestor.ts`, requiring use of `ReturnType<typeof createStore>` for store type inference.
- `ClassOverview.test.tsx` timing issue: Component rendered before SubjectStore finished auto-loading. Fixed by increasing waitFor timeout.

**Decisions Made:**
- Created `createEntityStore<T, TCreate>()` factory that generates standardized stores with:
  - `entities[]` and `loading` state
  - `loadAll()` function with try/catch/finally pattern
  - `create()` function that pushes to state array
  - Auto-load on init (configurable)
  - Consistent Redux DevTools action naming
- Refactored ClassStore, StudentStore, and SubjectStore to use the factory:
  - Reduced from ~69 lines each to ~20 lines each
  - Total reduction: 207 → 60 lines (71% reduction)
  - Maintained identical exported API for backward compatibility
- Created `useEntityStore()` helper and `createEntityStoreHook()` for customized hooks
- Included comprehensive inline tests using Vitest's `import.meta.vitest` pattern
- Kept AssessmentStore, AssessmentGradeStore, ChangeLogStore, and RecentAssessmentStore unchanged (they have unique logic not suitable for the generic factory)

**Technical Info Consulted:**
- AGENTS.md guidelines for code organization and testing
- Existing store implementations to identify common patterns
- Vitest documentation for inline tests with `import.meta.vitest`

**Assumptions Made:**
- Simple CRUD stores follow a consistent pattern suitable for factory generation
- Stores with complex logic (upserts, filtering, sorting, limits) should remain custom implementations
- Backward compatibility is important - existing imports should continue to work

**Other Notes:**
- Files created: `src/shared/store/createEntityStore.ts` (293 lines with tests)
- Files modified:
  - `src/features/administration/classes/ClassStore.ts` (69→20 lines)
  - `src/features/administration/students/StudentStore.ts` (69→20 lines)
  - `src/features/administration/subjects/SubjectStore.ts` (69→20 lines)
  - `src/features/administration/classes/ClassStore.test.ts` (updated setup)
  - `src/features/administration/students/StudentStore.test.ts` (updated setup)
  - `src/features/administration/subjects/SubjectStore.test.ts` (updated setup)
  - `src/features/administration/students/ClassOverview.test.tsx` (increased timeout)
- All 188 tests pass
- `pnpm run check` (typecheck + prettier + eslint + tests) passes
- Code savings: ~140 lines eliminated across 3 store files

### 19:20 - Simplify loading pattern with combineLoading utility [opencode/kimi-k2.5-free]

**User Prompt:**
simplify the loading pattern/loading state

**Issues Encountered:**
- Initially removed `assessmentsLoading` from AssessmentPage but it was still used in a useEffect dependency for tracking assessment usage. Had to restore it.

**Decisions Made:**
- Created a simple `combineLoading()` utility function that takes multiple boolean loading states and returns true if any is true
- This simplifies the repetitive pattern of OR-ing multiple loading states manually
- Updated 4 pages to use the new utility: AssessmentPage, StudentGradesPage, SubjectOverview, and ClassOverview
- The utility is colocated with store utilities in `shared/store/combineLoading.ts` and includes inline tests

**Technical Info Consulted:**
- Existing loading patterns across the codebase use manual OR operations like:
  ```typescript
  const isLoading = classesLoading || studentsLoading || subjectsLoading || ...
  ```
- This pattern appears in multiple pages that aggregate data from multiple stores

**Assumptions Made:**
- Pages should only pass loading states that are actually used for UI blocking (e.g., table loading states)
- Individual store loading states that are only used for side effects (like tracking) should still be destructured separately

**Other Notes:**
- The utility function is variadic, accepting any number of boolean arguments
- Tests cover edge cases: all false, any true, all true, single state, and no states

### 19:16 - Add inline editing to table views [Claude 3.5 Sonnet]

**User Prompt:**
Allow editing classes, students, subjects and assessments inline in the table views

**Issues Encountered:**
- Out of memory errors during full test suite runs - resolved by running targeted tests

**Decisions Made:**
- Implemented inline editing pattern using local state (editingKey, editingField states) in each table component
- Added update methods to stores (ClassStore, StudentStore, SubjectStore) that wrap repository update calls and update local state
- Used existing repository update methods which already supported partial updates via Zod schemas

**Technical Info Consulted:**
- Examined existing table implementations (ClassTable, StudentTable, SubjectTable, AssessmentTable) to understand current structure
- Reviewed Repository interface and createEntityStore pattern for state management
- Referenced AssessmentStore which already had updateAssessment implemented

**Assumptions Made:**
- The inline editing pattern should work similarly across all tables
- Users prefer a "Bearbeiten" button to enter edit mode, with "Speichern" and "Abbrechen" buttons to confirm/cancel
- Error handling should show user-friendly German messages via antd message component

**Other Notes:**
- All tests pass (188 tests)
- TypeScript and lint checks pass
- Format check passes
- Inline editing now available in: ClassTable, StudentTable, SubjectTable, AssessmentTable
