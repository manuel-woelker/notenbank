### 08:14 - Fix sidebar active highlighting for non-leaf items [openai/gpt-5-codex]

**User Prompt:**
Fix the sidebar active item highlighting. It currently only highlights leaf items. It should always highlight the most specific item in the sidebar, even when it is not a leaf node.

**Issues Encountered:**
- Ant Design `Menu` applies selected styling reliably to leaf items, but submenu titles with children did not show the same active visual state when selected via `selectedKeys`.
- Sidebar route-to-key mapping still used legacy keys for `/content` and `/upload`, while the current sidebar exposes `dashboard`, `classes`, and `changelog`.

**Decisions Made:**
- Added an explicit active CSS class (`nb-sidebar-item-active`) to menu items based on `sidebarContext.selectedKey` in `useMenuItems.tsx`.
- Applied CSS rules for both leaf items and submenu titles so non-leaf active nodes are visibly highlighted.
- Updated `resolveSidebarContext` to map `/aenderungsverlauf` to `changelog` and to fall back unknown top-level routes to `dashboard`.
- Extended `RootLayout.test.ts` to assert changelog selection and aligned the existing top-level fallback expectation.

**Technical Info Consulted:**
- `ui/src/routes/layouts/useMenuItems.tsx`
- `ui/src/routes/layouts/resolveSidebarContext.ts`
- `ui/src/routes/layouts/RootLayout.test.ts`
- `ui/src/index.css`

**Assumptions Made:**
- The intended active behavior for routes not represented by explicit sidebar items is to highlight `dashboard`.
- Visual active styling for non-leaf nodes should match the selected item color used by Ant Design dark menu (`#1677ff`).

**Other Notes:**
- Ran `../tool-tool.exe pnpm run format` and `../tool-tool.exe pnpm run check` in `ui`; all checks passed (`43` test files, `204` tests).

### 08:16 - Fix sidebar context resolution data source [openai/gpt-5-codex]

**User Prompt:**
The sidebar does not expand anymore beyond classes. Active highlighting is still broken.

**Issues Encountered:**
- Sidebar context in `RootLayout` was resolved with empty class/subject/assessment arrays, so current route entities could never be resolved.
- Without resolved entities, class/subject submenus could not be built as expected and active selection stayed too generic.

**Decisions Made:**
- Wired `RootLayout` to `useClassStore`, `useSubjectStore`, and `useAssessmentStore` and passed real data into `resolveSidebarContext`.
- Kept existing non-leaf active styling and route-key mapping changes from the previous fix.

**Technical Info Consulted:**
- `ui/src/routes/layouts/RootLayout.tsx`
- `ui/src/routes/layouts/resolveSidebarContext.ts`
- `ui/src/routes/layouts/useMenuItems.tsx`

**Assumptions Made:**
- The intended behavior is route-driven expansion/highlighting based on currently loaded entities.

**Other Notes:**
- Re-ran `../tool-tool.exe pnpm run format` and `../tool-tool.exe pnpm run check` in `ui`; all checks passed (`43` test files, `204` tests).

### 08:30 - Keep active non-leaf sidebar highlight on hover [openai/gpt-5-codex]

**User Prompt:**
When hovering over an active, non-leaf entry, the highlighting disappears. fix it

**Issues Encountered:**
- Hover styles from Ant Design submenu titles overrode the custom active background for non-leaf items.

**Decisions Made:**
- Extended the active sidebar CSS selectors to include hover states for both active menu items and active submenu titles.
- Kept the same active color to avoid visual mismatch between idle and hover states.

**Technical Info Consulted:**
- `ui/src/index.css`

**Assumptions Made:**
- Active entries should retain the same background color while hovered.

**Other Notes:**
- Ran `../tool-tool.exe pnpm run format` and `../tool-tool.exe pnpm run check` in `ui`; all checks passed (`43` test files, `204` tests).

### 08:40 - Add notenlinie (grading curve) to example assessment data opencode/kimi-k2.5-free

**User Prompt:**
In the /classes/5a/subjects/mathematik/assessments/schriftlich-1 example data use the notenlinie feature for grading

**Issues Encountered:**
- The grading curve wasn't showing up in the UI because TinyBase doesn't support storing nested objects directly
- The `gradingCurve` object was being lost during serialization to IndexedDB
- The `Serialized<T>` type in `RepositoryTypes.ts` only handled Date fields, not nested objects

**Decisions Made:**
- Added grading curve to the "Schriftlich 1" assessment for Mathematik in class 5a
- Used points-based grading mode with 40 points for grade 1 and 20 points for grade 4
- Added custom `serialize` and `deserialize` functions to `AssessmentRepository` to convert the `gradingCurve` object to/from JSON strings
- Updated `TinyBaseRepository.RepositoryConfig` to allow flexible serialization types with `any` type annotations
- This approach allows nested objects to be stored as JSON strings while maintaining type safety at the entity level

**Technical Info Consulted:**
- Reviewed `AssessmentTypes.ts` to understand the GradingCurveConfig structure
- Reviewed `GradingCurve.ts` to understand how grading curves work
- Reviewed `AssessmentRepository.ts` to understand how assessments are stored
- Examined `exampleDatabaseSeed.ts` to understand the seed data structure
- Reviewed `TinyBaseRepository.ts` to understand serialization/deserialization

**Assumptions Made:**
- The user wanted a points-based grading curve (not errors-based) for the example
- Grade 1 threshold: 40 points (best grade)
- Grade 4 threshold: 20 points (passing grade)
- This creates a linear interpolation between these anchor points

**Other Notes:**
- Modified `SeedAssessment` type to include optional `gradingCurve` property
- Updated `buildAssessmentsForSubject` function to add grading curve to `mathe-5a-written-1`
- Updated the seeding logic to pass `gradingCurve` when creating assessments
- Added custom serialization in `AssessmentRepository` to handle the gradingCurve object
- Created a test to verify the grading curve is properly seeded
- All existing tests pass (43 test files, 205 tests), code is properly formatted

### 09:00 - Fill in points and compute grades via grading curve opencode/kimi-k2.5-free

**User Prompt:**
for this assessment, the points should be filled in for each assessmentgrade, and then the grade computed via the grading curve

**Issues Encountered:**
- The existing seed data only had grades, not the raw points that were used to compute them
- Needed to demonstrate the full workflow where points are entered and grades are calculated automatically

**Decisions Made:**
- Updated `SeedAssessmentGrade` type to include optional `points` field
- Created a `generatePointsForMathe5a` function that generates points ranging from 0 to 40 in varying increments
- Used `calculateGradeFromCurve` function to compute grades from the generated points
- Special handling for `mathe-5a-written-1` assessment to generate points and compute grades
- Updated the seeding logic to pass `points` when creating assessment grades

**Technical Info Consulted:**
- `AssessmentGradeTypes.ts` for the grade structure with points field
- `GradingCurve.ts` for `calculateGradeFromCurve` function
- `exampleDatabaseSeed.ts` for the seed data generation logic

**Assumptions Made:**
- Points should vary to show different grade outcomes (0 to 40 range)
- Used a points cycle: [0, 5, 10, 15, 20, 25, 30, 35, 40, 38, 32, 28] to demonstrate various scenarios
- Grades are computed automatically from points using the grading curve formula

**Other Notes:**
- Modified seed data generation to check if assessment has grading curve and generate points accordingly
- For mathe-5a-written-1: generates points and computes grades via `calculateGradeFromCurve`
- For other assessments: keeps the existing grade cycle logic
- Updated the assessment grade creation to include `points: seed.points ?? null`
- All tests pass (43 test files, 205 tests)
